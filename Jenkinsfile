pipeline {
    agent any
    /* options { 
	skipDefaultCheckout() 
    } */

    stages {
        stage('Build') {
            steps {                
                echo 'Hello World'
            }
        }
    }
}
